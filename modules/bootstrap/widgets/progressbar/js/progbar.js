function pb_set_procent(pbid,proc)
{
	$('#'+pbid+'_inside').style('width',proc+'%');
}

function pb_show(id)
{
	$('#'+pbid+'_inside').show();
} 

function pb_hide(id)
{
	$('#'+pbid+'_inside').hide();
} 